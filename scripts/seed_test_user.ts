// Seed a predictable user via Supabase client if your app needs it.
// Leave as a no-op if the UI only needs the auth_test cookie set by middleware.

console.log("Seed script: no-op (auth_test cookie provides identity in E2E).");
